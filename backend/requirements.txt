# AVR-25D — backend Python dependencies
#
# Replaces a stray `pip freeze` of an unrelated global environment (168 of its
# ~180 pins were not installed here and nothing in the project imported them —
# ultralytics, catboost, xgboost, kaggle, tree-sitter for twenty languages, and
# a package called `nupy`, which is a typosquat of numpy and should not be
# installed anywhere).  Every line below is either imported by this repository
# or named in IMPLEMENTATION_PLAN.md §2.1.
#
# INTERPRETER: Python 3.14.  IMPLEMENTATION_PLAN.md §2.1 says ">= 3.10, < 3.13"
# and pins numpy 1.26.4 / scipy 1.13.1; neither builds on 3.14.  Raised Day 3,
# settled here in favour of refreshing the pins rather than pinning the
# interpreter back to 3.12: the whole suite is green on 3.14 with the versions
# below, and downgrading would mean re-verifying the ONNX export, the k-NN
# reprojection and the 971-scan label cache to buy nothing.  §2.1 has been
# updated to match.
#
# The pinned versions are the ones the perception work was actually measured
# on.  Do not upgrade during the sprint (§2.1).
#
#   python -m venv backend/.venv
#   backend/.venv/bin/python -m pip install -r backend/requirements.txt
#   backend/.venv/bin/python -m pip install -e model/

# --- core pipeline · imported by avr25d/ -----------------------------------
numpy==2.5.2
scipy==1.18.1              # cKDTree for k-NN label reprojection, RANSAC helpers
PyYAML==6.0.3              # config.yaml loader

# --- perception inference · avr25d/perception/onnx_infer.py ----------------
onnx==1.22.0
onnxruntime==1.29.0        # CPU execution provider only

# --- benchmarks · avr25d/bench/memory.py (§6.11) ---------------------------
psutil==7.2.2              # peak-RSS measurement

# --- server · avr25d/server/ (§6.12, Anuj) ---------------------------------
fastapi==0.141.1
uvicorn==0.52.4
websockets==17.1
pymongo==4.17.0            # official driver, no ODM (§2.1)

# --- tooling ---------------------------------------------------------------
certifi==2026.7.22         # tools/fetch_kitti.py — python.org builds ship no CA store
pytest==9.1.1

# --- export only · tools/export_onnx.py, not a runtime dependency ----------
# Large (~2 GB with its CUDA-less wheels) and needed once, to turn the
# lidar-bonnetal checkpoint into ONNX.  Install on demand:
#   pip install torch==2.13.0
# torch==2.13.0
